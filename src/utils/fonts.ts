import { Monda, Roboto, Inter } from 'next/font/google'

export const monda = Monda({
  subsets: ['latin'],
})
export const roboto = Roboto({
  subsets: ['latin'],
})
export const inter = Inter({
  subsets: ['latin'],
})